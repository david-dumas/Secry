import 'package:flutter/cupertino.dart';

class AccountLoggedoutPage extends StatelessWidget {
  const AccountLoggedoutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
